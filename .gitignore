# -----------------------------------------------------------------------------
# Secrets and generated configuration
#
# .env is generated per installation and holds POSTGRES_PASSWORD,
# SESSION_SECRET, SMTP_PASS and DATABASE_URL. It must never be committed,
# and neither must the timestamped backups the installer writes next to it.
# templates\env.template is deliberately NOT matched by these patterns.
# -----------------------------------------------------------------------------

.env
*.env
.env.*
!.env.example
.delta-install.json

# -----------------------------------------------------------------------------
# Certificates and private key material
# -----------------------------------------------------------------------------

certs/
*.pfx
*.p12
*.key
*.pem
*.crt
*.cer

# -----------------------------------------------------------------------------
# Installer transcripts, container logs, backups
# -----------------------------------------------------------------------------

logs/
*.log
backups/
*.dump
*.sql.gz

# -----------------------------------------------------------------------------
# Vendor installer binaries staged for an offline install (Docker Desktop).
# Resolve-DeltaDockerInstaller looks here; the binaries are never committed.
# -----------------------------------------------------------------------------

installers/
*.exe
*.msi

# -----------------------------------------------------------------------------
# Release packaging output.
#
# tools\build-release.ps1 deletes and recreates release\ wholesale on every
# run. It is build output, never source - and leaving it untracked but not
# ignored would dirty the working tree and trip release.ps1's clean-tree
# guardrail after any local package build.
# -----------------------------------------------------------------------------

release/

# -----------------------------------------------------------------------------
# Local test artefacts and scratch output
# -----------------------------------------------------------------------------

.scratch/
tmp/
temp/
*.tmp
*.temp
*.bak
*.bak-*
*.orig

# -----------------------------------------------------------------------------
# Editors / tooling
# -----------------------------------------------------------------------------

.vscode/
.idea/
.claude/
*.swp
*.swo

# -----------------------------------------------------------------------------
# Windows / macOS shell artefacts
# -----------------------------------------------------------------------------

Thumbs.db
Desktop.ini
.DS_Store
